name: Lunarbit Dependency Review

on:
  pull_request:

permissions:
  contents: read

jobs:
  dependency-review:
    name: Review dependency changes
    runs-on: ubuntu-latest
    steps:
      - name: Review dependency diff
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high
